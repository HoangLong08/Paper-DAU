% =============================================================================
% §11 CONCLUSION
% ⚠️ SCOPE DISCIPLINE (ke-hoach §4): the conclusion is NOT "metaheuristics are
% useless in general". It is bounded to THIS problem: on this task, with this
% objective, the optimization is already solved exactly, the variable being
% optimized has limited influence on the scored mask, and the reported criteria
% move against the clinical score along the threshold-count axis.
% ⚠️ Nothing here may state a result that is not already stated, with provenance,
% in §5-§9. The conclusion introduces no numbers of its own.
% ⚠️ Banned strings (submission gate, ke-hoach §6): "quantum advantage",
% "quantum speedup", "quantum parallelism", "we establish the ceiling", and any
% "outperforms"/"superior" not immediately backed by a stated test.
% =============================================================================
\section{Conclusion}
\label{sec:conclusion}

\TODO{paragraph 1 --- what was measured, in one sentence per result section,
each traceable to a table or figure above and to a provenance row in the results
ledger. No new numbers.}

\TODO{paragraph 2 --- the bounded claim. On this problem, an exact solution has
existed for over a decade~\cite{menotti_ciarp}, the attainable performance of
the method family is bounded and that bound is already
published~\cite{francois_jmiv}, and what our measurements add is the projection
between them: whether the optimization distance the field competes over reaches
the clinical mask at all. State the outcome of that projection as an observation
about the task, not as a verdict about a community.}

\TODO{paragraph 3 --- the constructive part: the decomposition, the
histogram-time diagnostic that lets any author bound their method family before
building an optimizer, and the protocol checklist. Restate that neither the exact
solver nor the underlying bound result is claimed as ours.}

\TODO{paragraph 4 --- what would change the conclusion: criteria that are not
interval-additive, decoders outside the intensity-only family, volumetric or
multi-modal inputs. Name the experiment that would overturn each claim, in the
spirit of the refutation-test framing of the evaluation.}
